# 《人工智能导论》实验二：情感分析报告

**学号：** (请在此处填写你的学号)
**姓名：** (请在此处填写你的姓名)
**日期：** (请在此处填写提交日期)

## 模型结构与流程分析

### 卷积神经网络 (CNN)

**结构图参考:** (实验说明中的图 2)

```
Input Text -> Embedding Layer -> Convolutional Layers (with different filter sizes) + GELU Activation -> Max Pooling -> Concatenation -> Dropout -> Fully Connected Layer -> Output (Logits)
```

**流程分析:**

基于 `src/model.py` 中的 `CNN` 类实现，其主要流程如下：

1.  **嵌入层 (Embedding Layer):**

    - 将输入的文本序列（词语索引）映射为密集向量表示。
    - 可以加载预训练的词向量（如 Word2Vec, GloVe），并选择是否在训练过程中微调（fine-tune）或冻结（freeze）嵌入层权重。在我们的实现中，如果提供了预训练向量，则会加载，并根据 `freeze_embeddings` 参数决定是否冻结。
    - `PAD_IDX` 用于处理变长序列，使得所有序列在批处理时具有相同的长度，嵌入层会忽略填充部分的计算。

2.  **卷积层与激活 (Convolutional Layers & Activation):**

    - 模型采用多个并行的卷积层，每个卷积层使用不同尺寸的卷积核（`filter_sizes`）。这允许模型捕捉不同长度的 n-gram 特征。
    - 每个卷积核在嵌入后的文本上进行一维卷积操作（`nn.Conv2d` 通过将输入视为高度为 1 的图像实现，宽度为`embedding_dim`）。`in_channels` 为 1（因为文本嵌入被视为单通道），`out_channels` 为 `num_filters`。
    - 卷积操作后，紧接着应用 GELU 激活函数 (`nn.GELU()`)。GELU 是一种平滑的激活函数，近年来在 Transformer 等模型中表现优异。

3.  **池化层 (Pooling Layer):**

    - 对每个卷积和激活后的输出进行最大池化（`F.max_pool1d`）。池化操作沿着序列长度维度进行，提取每个卷积核产生的特征图中最显著的特征。
    - 池化后的结果是一个固定长度的向量，其长度等于卷积核的数量 (`num_filters`)。

4.  **特征拼接与输出 (Concatenation & Output):**
    - 将所有不同尺寸卷积核产生的池化后的特征向量进行拼接（`torch.cat`），形成一个更丰富的组合特征向量。
    - 拼接后的向量经过一个 Dropout 层，以减少过拟合。
    - 最后，通过一个全连接层（`nn.Linear`）将组合特征映射到最终的输出维度（对于二分类问题，`output_dim` 通常为 1，表示正向情感的 logit 值）。

### 循环神经网络 (RNN) - GRU

**结构图参考:** (实验说明中的图 3, 将 LSTM 单元替换为 GRU 单元概念)

```
Input Text -> Embedding Layer -> GRU Layer (possibly multi-layer and bidirectional) -> Dropout -> Layer Normalization -> Fully Connected Layer -> Output (Logits)
```

**流程分析:**

基于 `src/model.py` 中的 `RNN` 类实现，使用门控循环单元（GRU），其主要流程如下：

1.  **嵌入层 (Embedding Layer):**

    - 与 CNN 模型中的嵌入层作用相同，将输入文本序列转换为词向量序列。同样支持预训练词向量和权重冻结。

2.  **GRU 层 (GRU Layer):**

    - GRU 是一种改进的 RNN 单元，通过更新门和重置门来解决传统 RNN 中的梯度消失问题，并能更好地捕捉序列中的长距离依赖关系。
    - 模型可以配置 GRU 的层数（`num_layers`）和是否双向（`bidirectional`）。
      - **多层 GRU:** 将前一层的输出作为后一层的输入，使模型能学习更抽象的特征表示。
      - **双向 GRU:** 同时从前向和后向处理序列，能够综合利用上下文信息。如果使用双向 GRU，则每个时间步的输出是前向和后向隐藏状态的拼接。
    - GRU 层会输出所有时间步的隐藏状态以及最后一个时间步的隐藏状态。对于分类任务，我们通常使用最后一个时间步的隐藏状态作为整个序列的表示。
    - 如果 `num_layers > 1`，GRU 内部会应用 dropout。

3.  **隐藏状态处理与输出 (Hidden State Processing & Output):**
    - 从 GRU 层获取最终的隐藏状态。
      - 如果 RNN 是双向的（`self.gru.bidirectional` 为 `True`），则拼接最后一个时间步的前向隐藏状态 (`hidden[-2,:,:]`) 和后向隐藏状态 (`hidden[-1,:,:]`)。
      - 如果 RNN 是单向的，则直接取最后一个时间步的隐藏状态 (`hidden[-1,:,:]`)。
    - 处理后的隐藏状态经过一个 Dropout 层。
    - 之后，应用一个层归一化（`nn.LayerNorm`）层。Layer Normalization 对每个样本的特征进行归一化，有助于稳定 RNN 的训练，特别是在处理变长序列时。
    - 最后，通过一个全连接层（`nn.Linear`）将归一化后的隐藏状态映射到输出维度（对于二分类问题为 1）。

### (可选) 多层感知机 (MLP) - Baseline

**结构描述:**

```
Input Text -> Embedding Layer -> Averaging/Pooling -> Fully Connected Layer 1 (with BatchNorm & GELU) -> Dropout -> Fully Connected Layer 2 -> Output (Logits)
```

**流程分析:**

基于 `src/model.py` 中的 `MLP` 类实现，其主要流程如下：

1.  **嵌入层 (Embedding Layer):**

    - 与 CNN 和 RNN 模型中的嵌入层作用相同。

2.  **句子嵌入表示 (Sentence Embedding):**

    - 为了将可变长度的词向量序列转换为固定长度的句子表示，MLP 模型采用了一种简单而有效的方法：对序列中所有非填充词的词向量进行平均。
    - 通过创建一个掩码（`mask`）来识别非填充词（`PAD_IDX`），将填充词的向量置零，然后计算所有词向量的总和，并除以非填充词的数量，得到句子的平均嵌入。

3.  **全连接层 (Fully Connected Layers):**
    - 句子的平均嵌入向量首先通过第一个全连接层（`self.fc1`），将其映射到一个隐藏维度（`hidden_dim`）。
    - 之后，应用批归一化（`nn.BatchNorm1d`）和 GELU 激活函数 (`F.gelu()`)。
    - 再经过一个 Dropout 层。
    - 最后，通过第二个全连接层（`self.fc2`）将隐藏表示映射到最终的输出维度。

## 实验设置

### 数据集描述

实验数据集包含电影评论文本，已预先分词，并区分为训练集、验证集和测试集。任务是进行二分类（正向/负向情感）。预训练的词向量也已提供。

### 预处理

- **分词:** 数据已完成分词。
- **词向量:** 使用提供的预训练词向量初始化模型的嵌入层。可以选择在训练过程中冻结或微调嵌入层权重。
- **序列填充:** 对每个批次内的序列进行填充（padding），使其达到该批次中最长序列的长度，以便进行批处理。`PAD_IDX` 标记用于填充。

### 超参数设置

(请在此处列出你为 CNN, RNN, MLP 模型选择的主要超参数，例如：)

- **通用参数:**
  - `embedding_dim`: (e.g., 100, 与预训练词向量维度一致)
  - `output_dim`: 1 (for binary classification logits)
  - `dropout`: (e.g., 0.5)
  - `learning_rate`: (e.g., 1e-3, 1e-4)
  - `batch_size`: (e.g., 64, 128)
  - `num_epochs`: (e.g., 20, 或由早停决定)
  - `optimizer`: (e.g., Adam, AdamW)
  - `criterion`: `nn.BCEWithLogitsLoss`
  - `freeze_embeddings`: (True or False)
- **CNN 特定参数:**
  - `num_filters`: (e.g., 100, 128)
  - `filter_sizes`: (e.g., [3, 4, 5])
- **RNN (GRU)特定参数:**
  - `hidden_dim`: (e.g., 128, 256)
  - `num_layers`: (e.g., 1, 2)
  - `bidirectional`: (True or False)
- **MLP 特定参数:**
  - `hidden_dim`: (e.g., 128, 256)

## 实验结果与分析

(请在此处填充你的实验结果。建议使用表格形式展示不同模型在测试集上的准确率和 F1 分数。)

**评价指标:**

- **准确率 (Accuracy):** `correct_predictions / total_predictions`
- **F1-score:** `2 * (Precision * Recall) / (Precision + Recall)`

**表 1: 不同模型性能对比**

| 模型类型  | 准确率 (Accuracy) | F1 分数 (F1-score) | Precision      | Recall         |
| :-------- | :---------------- | :----------------- | :------------- | :------------- |
| CNN       | (填写你的结果)    | (填写你的结果)     | (填写你的结果) | (填写你的结果) |
| RNN (GRU) | (填写你的结果)    | (填写你的结果)     | (填写你的结果) | (填写你的结果) |
| MLP       | (填写你的结果)    | (填写你的结果)     | (填写你的结果) | (填写你的结果) |

**结果对比与讨论:**
(在此处分析上表中的结果。例如：)

- 哪个模型表现最好？哪个最差？
- CNN 和 RNN 相比，在当前任务上的表现有何差异？可能的原因是什么？（例如，CNN 可能更擅长捕捉局部关键短语，而 RNN 可能更擅长理解序列依赖和上下文信息。）
- 与基线 MLP 模型相比，CNN 和 RNN 的提升是否显著？
- 分析模型的训练时间差异。RNN 由于其序列依赖性，通常训练时间会长于 CNN 和 MLP。

## 参数比较与分析

(在此部分，请详细讨论你在实验过程中调整过的不同参数（如学习率、批大小、dropout 率、模型特定参数如卷积核大小/数量、RNN 层数/隐藏单元数等）对模型性能（准确率、F1 分数、训练稳定性、收敛速度等）的影响，并分析原因。)

**示例：**

- **学习率 (Learning Rate):**
  - 尝试了不同的学习率（如 1e-2, 1e-3, 1e-4）。
  - 观察到学习率过高（1e-2）可能导致训练不稳定，损失函数震荡或不收敛。
  - 学习率过低（1e-4）可能导致收敛速度过慢，需要更多训练轮次。
  - 最终选择 1e-3 作为较优的学习率，因为它能在合理的时间内使模型稳定收敛到较好的性能。
- **Dropout 率:**
  - 比较了不同的 dropout 率（如 0.2, 0.5, 0.7）。
  - 较高的 dropout 率（0.7）可能导致欠拟合，模型学习能力不足。
  - 较低的 dropout 率（0.2）可能不足以防止过拟合，尤其是在模型较复杂或数据量较少时。
  - 0.5 的 dropout 率在多数情况下提供了一个较好的平衡。
- **CNN Filter Sizes:**
  - 实验了不同的卷积核组合，如 `[3,4,5]` vs `[2,3,4]` vs `[3,5,7]`。
  - 组合 `[3,4,5]` 通常能捕捉到不同长度的关键短语，表现较为均衡。
  - (具体分析你的实验观察)
- **RNN Bidirectionality:**
  - 比较了单向 GRU 和双向 GRU 的性能。
  - 双向 GRU 通常能取得更好的结果，因为它能同时考虑过去和未来的上下文信息，但计算成本也更高。
  - (具体分析你的实验观察)
- **(其他你调整过的参数...)**

## 问题思考

### 实验训练什么时候停止是最合适的？简要陈述你的实现方式，并试分析固定迭代次数与通过验证集调整等方法的优缺点。

**实现方式:**
在本次实验的代码 (`src/train.py`) 中，我们采用了 **早停（Early Stopping）** 机制来决定训练的停止时机。具体实现如下：

- 在每个 epoch 训练完成后，在验证集上评估模型的性能（通常是验证集损失 `val_loss`）。
- 如果验证集损失在连续 `patience` 个 epoch 内没有得到改善（即没有降低超过一个很小的阈值 `delta`），则提前终止训练。
- 在训练过程中，我们会保存验证集损失最低时的模型参数作为最佳模型。

**固定迭代次数的优缺点:**

- **优点:**
  - 实现简单，易于控制训练总时长。
  - 对于某些已知收敛特性的任务或模型，可以预估一个合适的迭代次数。
- **缺点:**
  - **可能欠拟合:** 如果迭代次数设置过少，模型可能没有充分学习数据中的模式，导致性能不佳。
  - **可能过拟合:** 如果迭代次数设置过多，模型可能会在训练数据上过度拟合，学习到噪声，导致在未见过的验证集或测试集上性能下降。
  - 难以找到一个通用的"最佳"迭代次数，它往往依赖于具体数据集、模型结构和超参数。

**通过验证集调整（如早停）的优缺点:**

- **优点:**
  - **防止过拟合:** 通过监控验证集性能，可以在模型开始过拟合（表现为验证集性能不再提升或开始下降）时及时停止训练。
  - **自动确定训练轮次:** 无需手动设置固定的迭代次数，模型会自动找到一个较优的训练点。
  - **通常能获得更好的泛化性能:** 因为模型是在验证集性能最优时停止的。
- **缺点:**
  - **对验证集质量敏感:** 如果验证集不能很好地代表测试数据的分布，或者验证集过小，早停可能不会带来预期的效果，甚至可能提前停止在次优点。
  - **增加了监控开销:** 需要在每个 epoch 后进行验证集评估，增加了训练时间。
  - **Patience 参数的选择:** `patience` 参数本身也需要调整，设置不当可能导致过早停止或训练时间过长。

总的来说，通过验证集调整（尤其是早停机制）是更常用且通常更有效的方法，因为它能更好地平衡欠拟合和过拟合，并倾向于找到泛化能力更强的模型。

### 实验参数的初始化是怎么做的？不同的方法适合哪些地方？

在我们的实验代码 (`src/model.py`) 中，参数初始化主要依赖于 PyTorch 各层（如 `nn.Embedding`, `nn.Linear`, `nn.Conv2d`, `nn.GRU`）的默认初始化策略，同时也提供了加载预训练词向量的选项。

- **`nn.Embedding`:**
  - 如果提供了 `pretrained_embedding`，则使用预训练的词向量进行初始化。这是一种非常有效的方法，因为它利用了大规模文本语料库预先学习到的语义信息。
  - 如果没有提供预训练向量，`nn.Embedding` 默认使用均值为 0，标准差为 1 的正态分布（`N(0,1)`）进行初始化。
- **`nn.Linear` 和 `nn.Conv2d`:**
  - PyTorch 的这些层通常采用基于 Kaiming He 初始化（针对 ReLU/GELU 激活函数）或 Xavier/Glorot 初始化（针对 Sigmoid/Tanh 激活函数）的变体。这些方法旨在使每一层的输出方差和梯度的方差保持在合理的范围内，从而加速收敛并避免梯度消失/爆炸问题。
  - 例如，`nn.Linear` 的权重默认使用均匀分布 `U(-sqrt(k), sqrt(k))` 初始化，其中 `k = 1 / in_features`。
- **`nn.GRU`:**
  - GRU（以及 LSTM）的权重和偏置也有其默认的初始化方式，通常是均匀分布或正态分布，旨在维持较好的梯度流。

**不同的初始化方法及其适用场景:**

1.  **零均值初始化 (Zero Initialization):**

    - 将所有权重初始化为 0。
    - **缺点:** 如果所有权重都为 0，那么在同一层的所有神经元将具有相同的输出和梯度，导致对称性问题，模型无法学习。通常不用于权重初始化，但可用于偏置项。
    - **适用:** 有时用于偏置项，或在特定网络结构中作为一种简化。

2.  **随机初始化 (Small Random Values):**

    - 从一个小范围的均匀分布或高斯分布中随机采样值作为权重（例如，`N(0, 0.01)`）。
    - **优点:** 打破对称性。
    - **缺点:** 如果随机值过大，可能导致激活值饱和（对 Sigmoid/Tanh）或梯度爆炸；如果过小，可能导致梯度消失。
    - **适用:** 简单的网络，但通常被更高级的方法取代。

3.  **Xavier/Glorot 初始化:**

    - 旨在使每层激活值的方差和反向传播时梯度的方差保持一致。
    - **Xavier Uniform:** 从 `U(-sqrt(6/(fan_in + fan_out)), sqrt(6/(fan_in + fan_out)))` 采样。
    - **Xavier Normal:** 从 `N(0, sqrt(2/(fan_in + fan_out)))` 采样。
    - `fan_in` 是输入单元数，`fan_out` 是输出单元数。
    - **适用:** 常用于 Sigmoid 和 Tanh 激活函数。

4.  **Kaiming He 初始化:**

    - Xavier 初始化的改进版，特别适用于 ReLU 及其变体（LeakyReLU, PReLU, GELU）。ReLU 等激活函数在负数区为 0 或接近 0，会改变激活值的分布。
    - **He Uniform:** 从 `U(-sqrt(6/fan_in), sqrt(6/fan_in))` 采样。
    - **He Normal:** 从 `N(0, sqrt(2/fan_in))` 采样。
    - **适用:** ReLU、GELU 及其变种激活函数。这是目前在 CNN 中非常常用的初始化方法。

5.  **正交初始化 (Orthogonal Initialization):**

    - 将权重矩阵初始化为随机正交矩阵。
    - **优点:** 有助于保持梯度的范数，特别是在 RNN 中可以缓解梯度消失/爆炸问题。
    - **适用:** 常用于 RNN 的权重初始化，有时也用于 CNN。

6.  **使用预训练权重 (Pre-trained Weights):**
    - 如实验中嵌入层使用预训练词向量。对于更深层的网络，可以使用在大型数据集（如 ImageNet 对图像，大规模文本对 NLP）上预训练好的模型权重。
    - **优点:** 显著加速收敛，通常能达到更好的性能，特别是在目标任务数据量较少时（迁移学习）。
    - **适用:** 几乎所有类型的模型，只要有合适的预训练模型可用。嵌入层使用预训练词向量是 NLP 任务中的标准做法。

选择哪种初始化方法取决于网络结构、激活函数类型以及是否有可用的预训练模型。PyTorch 的默认初始化通常是一个不错的起点。

### 过拟合是深度学习常见的问题，有什么方法可以防止训练过程陷入过拟合？

过拟合是指模型在训练数据上表现良好，但在未见过的测试数据或验证数据上表现较差的现象。模型学习到了训练数据中的噪声和特定模式，而不是普适的规律。防止过拟合的方法主要有：

1.  **获取更多数据 (Data Augmentation):**

    - 是解决过拟合最有效的方法之一。数据越多，模型越能学习到数据的真实分布。
    - 对于文本数据，数据增强的方法包括：
      - **回译 (Back Translation):** 将文本翻译成另一种语言再翻译回来。
      - **同义词替换 (Synonym Replacement):** 随机替换句子中的一些词为其同义词。
      - **随机插入/删除/交换 (Random Insertion/Deletion/Swap):** 随机在句子中插入、删除或交换词语。

2.  **正则化 (Regularization):**

    - 通过向损失函数添加惩罚项来限制模型复杂度。
    - **L1 正则化 (Lasso):** 添加权重的绝对值之和（`lambda * sum(|w|)`）到损失函数。倾向于产生稀疏权重（使一些权重变为 0），可以用于特征选择。
    - **L2 正则化 (Ridge / Weight Decay):** 添加权重的平方和（`lambda * sum(w^2)`）到损失函数。倾向于使权重值变小，但不会使其变为 0。在 PyTorch 的优化器中，`weight_decay` 参数就是 L2 正则化。
    - **Elastic Net:** L1 和 L2 正则化的结合。

3.  **Dropout:**

    - 在训练过程中，以一定的概率（`dropout_rate`）随机"丢弃"（即暂时将其输出置为 0）网络中的神经元。
    - 这迫使网络学习更鲁棒的特征，因为神经元不能依赖于其他特定神经元的存在。可以看作是训练多个简化模型的集成。
    - 在我们的 CNN、RNN 和 MLP 模型中都使用了 Dropout 层。

4.  **早停 (Early Stopping):**

    - 如前所述，在验证集上的性能不再提升时停止训练。
    - 这是防止过拟合非常实用且有效的方法。我们的 `src/train.py` 中已实现。

5.  **选择合适的模型复杂度 (Model Simplification):**

    - 如果模型过于复杂（例如，层数过多、神经元过多），它更容易过拟合。
    - 可以尝试减少网络层数、每层的单元数、CNN 中的滤波器数量、RNN 中的隐藏单元数等。

6.  **批归一化 (Batch Normalization) 和 层归一化 (Layer Normalization):**

    - **Batch Normalization (BN):** 对每一小批数据在激活函数之前进行归一化。可以加速训练，并具有一定的正则化效果（因为它为每个批次引入了轻微的噪声）。在我们的 MLP 模型中使用了 BN (CNN 中已移除)。
    - **Layer Normalization (LN):** 对单个样本的特征维度进行归一化。在 RNN 中比 BN 更常用，因为 RNN 处理变长序列，BN 在 RNN 中的应用有时不稳定。我们的 RNN 模型中也加入了 LN。
    - 这些归一化技术有助于稳定学习过程，并可能减少对其他正则化技术的依赖。

7.  **权重共享 (Weight Sharing):**

    - 例如 CNN 中的卷积核在整个输入图像/序列上共享权重，这本身就是一种有效的减少参数数量和控制模型复杂度的方式。

8.  **集成学习 (Ensemble Methods):**
    - 训练多个不同的模型，并将它们的预测结果结合起来（例如，通过投票或平均）。
    - 通常能获得比单个模型更好的性能和泛化能力。

在实践中，通常会组合使用上述多种方法来有效地防止过拟合。

### 试分析 CNN，RNN，全连接神经网络（MLP）三者的优缺点。

**1. 全连接神经网络 (MLP / Feedforward Neural Network):**

- **优点:**
  - **结构简单，易于理解和实现。**
  - **通用性强:** 理论上可以拟合任何复杂的函数。
  - **计算相对高效:** 如果网络层数和节点数不是特别巨大。
  - 对于某些特征工程做得很好的表格数据或简单序列数据（如将整个文本平均为一个向量），可以取得不错的效果。
- **缺点:**
  - **参数量大:** 如果输入维度很高（如直接处理高维图像或长文本序列的词袋模型），全连接层参数会非常多，容易过拟合，且计算成本高。
  - **缺乏对序列顺序或空间结构的利用:**
    - 对于文本，MLP 通常需要将变长序列转换为固定长度的向量（如平均词向量），这会丢失词序信息和局部上下文信息。
    - 对于图像，MLP 无法有效利用像素间的空间局部性。
  - **对输入维度敏感:** 输入维度变化时，通常需要重新设计网络结构。

**2. 卷积神经网络 (CNN):**

- **优点:**
  - **参数共享 (Weight Sharing):** 卷积核在整个输入上滑动并共享权重，大大减少了模型参数量，降低了过拟合风险，提高了计算效率。
  - **局部连接 (Local Connectivity):** 每个神经元只与输入的一个局部区域相连，使得模型能学习局部特征。这对于图像中的边缘、纹理，文本中的 n-gram 短语等局部模式的提取非常有效。
  - **平移不变性 (Translation Invariance):** 池化层使得模型对特征在输入中的位置不那么敏感。例如，一个短语无论出现在句首还是句中，CNN 都能识别它。
  - **层次化特征学习:** 通过堆叠卷积层和池化层，CNN 可以从低级简单特征（如边缘）学习到高级复杂特征（如物体部分或文本主题）。
  - 在文本分类任务中，CNN 能有效捕捉关键的局部短语和语义片段。
- **缺点:**
  - **对长距离依赖关系捕捉能力有限:** 标准 CNN 的感受野是有限的，对于需要理解整个序列长距离依赖关系的任务（如某些机器翻译或篇章理解），可能不如 RNN。
  - **对词序的敏感性不如 RNN:** 虽然可以通过不同尺寸的卷积核捕捉不同长度的 n-gram，但对于词序的精细变化不如 RNN 敏感。
  - **输入长度固定 (传统 CNN):** 虽然可以通过池化处理变长输入，但设计上更偏向于固定尺寸的输入。对于文本，通常需要 padding。

**3. 循环神经网络 (RNN - 包括 LSTM, GRU):**

- **优点:**
  - **擅长处理序列数据:** RNN 的设计使其能够处理变长序列，并通过循环连接在时间步之间传递信息，从而捕捉序列中的时间依赖关系和上下文信息。
  - **记忆能力:** LSTM 和 GRU 通过门控机制有效地解决了传统 RNN 的梯度消失/爆炸问题，能够学习和记忆序列中的长期依赖。
  - **对词序敏感:** RNN 按顺序处理输入，天然地考虑了词语的顺序。
  - **参数共享:** 在不同时间步共享相同的权重，使其能够推广到不同长度的序列。
- **缺点:**
  - **训练速度慢:** 由于其固有的序列依赖性（当前时间步的计算依赖于前一时间步的结果），RNN 难以进行大规模并行计算，导致训练和推理速度通常比 CNN 和 MLP 慢，尤其是在长序列上。
  - **梯度消失/爆炸问题 (虽然 LSTM/GRU 缓解了，但仍可能存在):** 在非常长的序列上，梯度问题仍可能出现，影响模型学习长期依赖的能力。
  - **对长距离依赖的捕捉仍有挑战:** 尽管 LSTM/GRU 有改进，但对于极长的序列（如整个文档），捕捉非常远距离的依赖关系仍然困难。Transformer 等模型在这方面表现更好。
  - **仅考虑单向上下文 (标准 RNN):** 标准 RNN 只考虑过去的上下文。双向 RNN（BiRNN）可以同时考虑过去和未来的上下文，但会增加计算复杂度。

**总结对比:**

| 特性             | MLP                                 | CNN                                      | RNN (LSTM/GRU)                             |
| :--------------- | :---------------------------------- | :--------------------------------------- | :----------------------------------------- |
| **主要应用场景** | 通用分类/回归，简单序列（平均化后） | 图像处理，文本分类（局部特征），语音识别 | 文本生成，机器翻译，语音识别，时间序列分析 |
| **序列信息处理** | 丢失词序（通常对输入做平均或展平）  | 捕捉局部 n-gram 特征，对词序不特别敏感   | 显式建模词序和时间依赖                     |
| **长距离依赖**   | 无法捕捉                            | 较弱（需特殊设计如空洞卷积）             | 较强（LSTM/GRU 设计目标之一）              |
| **参数量**       | 可能很大（取决于输入维度）          | 较少（权重共享）                         | 中等（时间步共享权重）                     |
| **并行计算能力** | 高                                  | 较高（卷积操作可并行）                   | 较低（序列依赖性）                         |
| **训练速度**     | 快                                  | 较快                                     | 慢                                         |

在情感分析任务中：

- **MLP** 作为基线，可以提供一个参考性能，但通常因为丢失了序列信息而表现不如 CNN 和 RNN。
- **CNN** 能有效提取情感相关的关键词或短语（如"excellent", "not good"），对于许多情感分类任务表现良好且高效。
- **RNN (GRU/LSTM)** 能更好地理解句子的整体语义、上下文和词序依赖，对于需要更深层次语义理解或处理否定、转折等复杂语言现象时可能更有优势，但训练成本更高。

选择哪个模型取决于具体的任务需求、数据特性、计算资源以及对性能和速度的权衡。

## 心得体会

(请在此处填写你通过本次实验获得的经验、教训、对情感分析任务的理解、对不同模型的认识、以及在编码和调试过程中遇到的问题和解决方法等。)

**例如：**

- 通过本次实验，我对 CNN 和 RNN 在文本分类任务中的应用有了更深入的理解...
- 在参数调优过程中，我发现学习率和 batch size 对模型收敛速度和最终性能有显著影响...
- 实现早停机制有效地避免了模型过拟合，并节省了训练时间...
- 比较不同模型时，注意到 GRU 虽然在某些情况下可能效果更好，但训练时间确实比 CNN 长很多...
- 数据预处理（如词向量的选择和是否冻结）对模型性能至关重要...
- 在调试 PyTorch 代码时，理解张量的维度变化和确保各层输入输出匹配是非常关键的...
- (任何其他你觉得值得分享的经验和思考)

## 附录 (可选)

(如果需要，可以在此附上关键代码片段的截图或文本，或对某些复杂部分的额外说明。)
